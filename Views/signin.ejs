<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign In - SkyReserve</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #e6e7ee;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .hero {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 50px;
        }

        .card {
            width: 30rem;
            box-shadow: 0 6px 10px rgba(0, 0, 0, 0.1);
            border: none;
            background-color: #ffffff;
        }

        .card-body {
            padding: 2rem;
        }

        h1.heading {
            color: #4A90E2;
            font-size: 2.5rem;
            font-weight: 700;
        }

        p {
            font-size: 1rem;
            color: #555;
        }

        .input-group {
            position: relative;
            margin-bottom: 20px;
        }

        input[type="text"] {
            padding: 10px 10px 10px 40px;
            border: 2px solid #ccc;
            border-radius: 5px;
            width: 100%;
            font-size: 1rem;
        }

        .input-group .fa {
            position: absolute;
            left: 15px;
            top: 15px;
            color: #555;
        }

        .login-button, .create_button {
            width: 100%;
            padding: 10px;
            font-size: 1rem;
            border-radius: 5px;
            cursor: pointer;
            border: none;
        }

        .login-button {
            background-color: #4A90E2;
            color: white;
        }

        .create_button {
            background-color: #34C759;
            color: white;
        }

        .login-button:hover, .create_button:hover {
            opacity: 0.9;
        }
    </style>
</head>

<body>
    <div class="hero">
        <div>
            <h1 class="heading">SkyReserve</h1>
            <p>Your Flight, Your Way, SkyReserve Today</p>
        </div>

        <div class="card text-center">
            <div class="card-body">
                <h1>Sign In</h1>
                <div class="input-group">
                    <i class="fa fa-user"></i>
                    <input type="text" placeholder="Email address or phone number" required>
                </div>
                <div class="input-group">
                    <i class="fa fa-lock"></i>
                    <input type="text" placeholder="Password" required>
                </div>
                <button class="login-button btn">Log in</button>
                <a href="/fogetPass" style="display: block; margin-top: 10px; color: #4A90E2;">Forget password?</a>
                <hr>
                <button class="create_button btn">Create an account</button>
            </div>
        </div>
    </div>
</body>

</html>
