<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Forget Password</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    /* Custom CSS */
    body {
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
      background-color: lightblue;
    }
    .form-container {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 400px;
      padding: 40px;
      border-radius: 10px;
      background: rgba(255, 255, 255, 0.95); /* Increased opacity for better readability */
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.2);
      animation: fadeIn 1s ease forwards;
    }
    .form-container h1 {
      text-align: center;
      margin-bottom: 30px;
      color: #292b2c; /* Darker color for better contrast */
    }
    .form-container form {
      display: flex;
      flex-direction: column;
    }
    .form-group {
      margin-bottom: 20px;
    }
    .form-group label {
      font-weight: bold;
      color: #333; /* Soft black for labels */
    }
    .form-group input {
      padding: 10px;
      border: 1px solid #bbbbbb; /* Lighter border for inputs */
      border-radius: 5px;
    }
    .form-text {
      font-size: 12px;
      color: #6c757d; /* Grey for muted text */
    }
    .btn-primary {
      padding: 10px 20px;
      background-color: #0056b3; /* More vibrant primary color */
      border: none;
      border-radius: 5px;
      color: #fff;
      cursor: pointer;
      transition: background 0.3s ease;
    }
    .btn-primary:hover {
      background-color: #004080; /* Darkened on hover for effect */
    }
    @media (max-width: 768px) {
      .form-container {
        width: 90%;
      }
    }
  </style>
</head>
<body>
  <section id="forget-password-container">
    <div class="form-container">
      <h1>Forget Password</h1>
      <form>
        <div class="form-group">
          <label for="email">Email address</label>
          <input type="email" class="form-control" id="email" aria-describedby="emailHelp" placeholder="Enter email">
          <small id="emailHelp" class="form-text text-muted">We'll send you an email to reset your password.</small>
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
      </form>
    </div>
  </section>
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
